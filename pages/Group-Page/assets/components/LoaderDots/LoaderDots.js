// Hide the loader dots when all images loaded.
function hideLoaderDots() {
    console.log("Loader dots hidden.");
    document.getElementsByClassName("loader").classList.add("hidden"); // Add the hidden class to the loader-dots-container.
}